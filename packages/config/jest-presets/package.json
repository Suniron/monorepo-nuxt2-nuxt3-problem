{
  "name": "jest-presets",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "ts-jest": "^29.0.3"
  },
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules"
  }
}
